## Admonish
To add the admonish plugin, the version of mdbook should be <0.5, I chose 0.4.52 (as seen in the CI). If mdbook is install with cargo. Simply use ```argo uninstall mdbook ``` and ```cargo install mdbook --version 0.4.52```.

Then use ```cargo install mdbook-admonish``` to install the plugin